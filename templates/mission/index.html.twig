{% extends 'base.html.twig' %}

{% block title %}Missions{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Missions</h1>
            <div>
                <a href="{{ path('app_super_hero_index') }}" class="btn btn-secondary me-2">
                    <i class="bi bi-house"></i> Accueil
                </a>
                <a href="{{ path('app_mission_cleanup') }}" class="btn btn-outline-danger me-2">
                    <i class="bi bi-trash"></i> Nettoyer les missions annulées
                </a>
                <form action="{{ path('app_mission_random') }}" method="post" class="d-inline me-2">
                    <input type="hidden" name="_token" value="{{ csrf_token('random_mission') }}">
                    <button type="submit" class="btn btn-warning">
                        <i class="bi bi-shuffle"></i> Mission aléatoire
                    </button>
                </form>
                <a href="{{ path('app_mission_new') }}" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Nouvelle mission
                </a>
            </div>
        </div>

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}

        <div class="row">
            <div class="col-md-8">
                <h2>Missions en cours</h2>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Titre</th>
                                <th>Difficulté</th>
                                <th>Pouvoirs requis</th>
                                <th>Statut</th>
                                <th>Héros assigné</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for mission in missions %}
                            {% if mission.status != 'completed' %}
                                <tr>
                                    <td>{{ mission.title }}</td>
                                    <td>
                                        {% for i in 1..mission.difficulty %}
                                            <i class="bi bi-star-fill text-warning"></i>
                                        {% endfor %}
                                    </td>
                                    <td>
                                        {% if mission.requiredPowers|length > 0 %}
                                            {% for power in mission.requiredPowers %}
                                                <span class="badge bg-info me-1">{{ power.name }}</span>
                                            {% endfor %}
                                        {% else %}
                                            <span class="text-muted">Aucun pouvoir requis</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if mission.status == 'pending' %}
                                            <span class="badge bg-info">En attente</span>
                                        {% elseif mission.status == 'in_progress' %}
                                            <span class="badge bg-warning">En cours</span>
                                        {% elseif mission.status == 'cancelled' %}
                                            <span class="badge bg-danger">Annulée</span>
                                            {% if mission.cancelledAt %}
                                                <br>
                                                <small class="text-muted">
                                                    Sera supprimée le {{ mission.cancelledAt|date_modify("+24 hours")|date('d/m/Y H:i') }}
                                                </small>
                                            {% endif %}
                                        {% endif %}
                                    </td>
                                    <td>
                                        {% if mission.superHero %}
                                            {{ mission.superHero.name }}
                                        {% else %}
                                            <span class="text-muted">Non assigné</span>
                                        {% endif %}
                                    </td>
                                    <td>
                                        <div class="btn-group">
                                            <a href="{{ path('app_mission_show', {'id': mission.id}) }}" class="btn btn-sm btn-info">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                            <form method="post" action="{{ path('app_mission_delete', {'id': mission.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette mission ?');" class="d-inline">
                                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission.id) }}">
                                                <button class="btn btn-sm btn-danger">
                                                    <i class="bi bi-trash"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            {% endif %}
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="col-md-4">
                <h2>Tableau des scores</h2>
                <div class="table-responsive">
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Mission</th>
                                <th>Héros</th>
                                <th>Score</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for mission in completed_missions %}
                            <tr>
                                <td>{{ mission.title }}</td>
                                <td>{{ mission.superHero.name }}</td>
                                <td class="text-end">{{ mission.score }}</td>
                                <td>
                                    <div class="btn-group">
                                        <a href="{{ path('app_mission_show', {'id': mission.id}) }}" class="btn btn-sm btn-info">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <form method="post" action="{{ path('app_mission_delete', {'id': mission.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette mission ?');" class="d-inline">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mission.id) }}">
                                            <button class="btn btn-sm btn-danger">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
