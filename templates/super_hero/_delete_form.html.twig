<form method="post" action="{{ path('app_super_hero_delete', {'id': super_hero.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce super-héros ?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ super_hero.id) }}">
    <button class="delete-btn">
        <i class="fas fa-trash"></i>
        Supprimer
    </button>
</form>