{% extends 'base.html.twig' %}

{% block title %}SuperHero{% endblock %}

{% block body %}
    <h1>SuperHero</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ super_hero.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ super_hero.name }}</td>
            </tr>
            <tr>
                <th>Alias</th>
                <td>{{ super_hero.alias }}</td>
            </tr>
            <tr>
                <th>Power</th>
                <td>{{ super_hero.power }}</td>
            </tr>
            <tr>
                <th>Weakness</th>
                <td>{{ super_hero.weakness }}</td>
            </tr>
            <tr>
                <th>Is_active</th>
                <td>{{ super_hero.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_super_hero_index') }}">back to list</a>

    <a href="{{ path('app_super_hero_edit', {'id': super_hero.id}) }}">edit</a>

    {{ include('super_hero/_delete_form.html.twig') }}
{% endblock %}
